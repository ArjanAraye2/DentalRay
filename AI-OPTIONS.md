# تحلیل رادیولوژی بدون هزینه — گزینه‌ها

**تاریخ:** ۳۱ شهریور ۱۴۰۵
**سؤال:** آیا می‌توان عکس‌های رادیولوژی را بدون هزینه برای OpenAI تحلیل کرد؟

**پاسخ کوتاه:** بله — سه راه هست. بهترینش **MedGemma** است که **کاملاً رایگان**
و **آفلاین** روی کامپیوتر خودتان اجرا می‌شود.

---

# ۱) وضعیت فعلی پروژه

## سه کنترلر AI وجود دارد

| فایل | کار |
|---|---|
| `AiRadiologyImageAnalysisController.cs` | تحلیل **یک تصویر** رادیولوژی |
| `AiStudyAnalysisController.cs` | تحلیل **همه‌ی تصاویر یک مطالعه** |
| `AiCardExtractionController.cs` | استخراج اطلاعات از **کارت ملی/بیمه** |

## همه‌شان OpenAI را صدا می‌زنند

```
آدرس   : https://api.openai.com/v1/responses
مدل    : gpt-5.6-sol  (پیش‌فرض در کد)
کلید   : OpenAI:ApiKey  در appsettings.json
```

## وضعیت فعلی

| مورد | وضعیت |
|---|---|
| کلید API | ❌ **تنظیم نشده** |
| رابط کاربری AI | ❌ **در index.html بارگذاری نمی‌شود** |
| نتیجه | قابلیت AI **غیرفعال** است |

**یعنی:** الان هزینه‌ای پرداخت نمی‌کنید چون استفاده نمی‌شود.

---

# ۲) گزینه‌ی اول — MedGemma (⭐⭐ بهترین)

## چیست؟

مدل **باز** گوگل که مخصوص **تصاویر پزشکی** آموزش دیده.
نسخه‌ی `4B` (چهار میلیارد پارامتر) روی کامپیوتر معمولی اجرا می‌شود.

## مزایا

| مزیت | توضیح |
|---|---|
| **کاملاً رایگان** | مجوز باز، بدون هزینه‌ی توکن |
| **آفلاین** | روی LAN کلینیک کار می‌کند |
| **حریم خصوصی** | تصویر بیمار **از کلینیک خارج نمی‌شود** |
| **مخصوص پزشکی** | روی تصاویر پزشکی آموزش دیده (نه عمومی) |
| **بدون محدودیت** | هر تعداد تصویر |

## معایب

| عیب | توضیح |
|---|---|
| نیاز به GPU | کارت گرافیک (حداقل ۸ گیگ حافظه) |
| نصب فنی | Python + مدل دانلود (~۸ گیگابایت) |
| سرعت | روی CPU کند است |

## چطور در پروژه استفاده شود

یک **سرویس محلی** بسازیم که مدل را اجرا کند، و کنترلرهای C# به
جای `api.openai.com` به `localhost` وصل شوند:

```
الان:   DentalRay.Api  →  api.openai.com          (پول + اینترنت)
بعد:    DentalRay.Api  →  localhost:8080 (MedGemma)  (رایگان + آفلاین)
```

**تغییر کد:** فقط آدرس و فرمت درخواست. سه کنترلر عوض می‌شوند.

---

# ۳) گزینه‌ی دوم — Gemini API (سطح رایگان)

## سهمیه‌ی رایگان (وضعیت ۲۰۲۶)

| مورد | مقدار |
|---|---|
| درخواست در دقیقه | ۵ تا ۱۵ |
| درخواست در روز | تا **۱۰۰۰** |

## مزایا

| مزیت | توضیح |
|---|---|
| **بدون نصب** | فقط یک کلید API |
| **بدون GPU** | روی سرور گوگل اجرا می‌شود |
| **سریع و دقیق** | کیفیت بالا |
| **۱۰۰۰ تصویر در روز** | برای کلینیک کافی است |

## معایب

| عیب | توضیح |
|---|---|
| **نیاز به اینترنت** | در کلینیک LAN بدون اینترنت کار نمی‌کند |
| **حریم خصوصی** | تصویر بیمار به گوگل می‌رود |
| **داده برای آموزش** | در سطح رایگان، گوگل ممکن است داده را استفاده کند |
| **محدودیت سهمیه** | اگر تمام شود، باید صبر کنید |

## ⚠️ هشدار حقوقی مهم

تصویر رادیولوژی = **داده‌ی پزشکی حساس**. فرستادنش به سرور خارجی
معمولاً **نیاز به رضایت کتبی بیمار** دارد و ممکن است با قوانین
حفظ داده‌ی پزشکی ایران **مغایر** باشد.

**برای کلینیک واقعی، گزینه‌ی آفلاین (MedGemma) بسیار ایمن‌تر است.**

---

# ۴) گزینه‌ی سوم — مدل‌های بینایی رایگان دیگر

| سرویس | سطح رایگان | دیدگاه |
|---|---|---|
| **Groq** | دارد، ولی بیشتر متن | برای تصویر محدود |
| **OpenRouter** | برخی مدل‌ها رایگان | کیفیت متغیر |
| **Hugging Face Inference** | محدود | کند |
| **LLaVA / Qwen-VL** | باز، آفلاین | عمومی، نه پزشکی |

**نتیجه:** برای تصویر پزشکی، **MedGemma** از همه بهتر است.

---

# ۵) مقایسه‌ی نهایی

| معیار | OpenAI (فعلی) | **MedGemma** | Gemini رایگان |
|---|---|---|---|
| هزینه | 💰 پول | ✅ **رایگان** | ✅ رایگان |
| اینترنت | لازم | ✅ **لازم نیست** | لازم |
| حریم خصوصی | ❌ می‌رود بیرون | ✅ **می‌ماند داخل** | ❌ می‌رود بیرون |
| GPU | لازم نیست | ❌ لازم | لازم نیست |
| نصب | آسان | ❌ فنی | آسان |
| آموزش پزشکی | عمومی | ✅ **مخصوص پزشکی** | عمومی |
| محدودیت تعداد | ندارد (با پول) | ✅ **ندارد** | ۱۰۰۰ در روز |

## 🏆 توصیه

### برای کلینیک واقعی
**MedGemma** — چون:
1. **حریم خصوصی بیمار حفظ می‌شود** (مهم‌ترین)
2. رایگان و بدون محدودیت
3. در LAN بدون اینترنت کار می‌کند
4. مخصوص تصاویر پزشکی آموزش دیده

### برای آزمایش سریع
**Gemini رایگان** — چون:
1. بدون نصب و GPU
2. پنج دقیقه‌ای راه می‌افتد
3. فقط برای **تست**، نه بیمار واقعی

---

# ۶) پیشنهاد عملی — سه مرحله

## مرحله ۱ — ساختار را آماده کنیم (بدون هزینه)

کد را طوری تغییر دهیم که **آدرس سرویس** قابل تنظیم باشد:

```json
{
  "AiProvider": {
    "BaseUrl": "https://api.openai.com/v1/responses",
    "ApiKey": "",
    "Model": "gpt-5.6-sol"
  }
}
```

**فایده:** هر سرویسی را بعداً بدون تغییر کد وصل کنید.

## مرحله ۲ — آزمایش با Gemini رایگان

فقط `BaseUrl` و `Model` و `ApiKey` را عوض کنید. **صفر تومان.**
(فقط برای تست، نه بیمار واقعی)

## مرحله ۳ — نصب MedGemma برای کلینیک

یک سرویس محلی + تغییر `BaseUrl` به `http://localhost:8080`.
**دیگر هیچ هزینه‌ای نیست.**

---

# ۷) نکته‌ی مهم درباره‌ی دقت

> **هیچ مدلی جای دندانپزشک را نمی‌گیرد.**

در متن فعلی کد خودتان نوشته شده:

```
"Do not claim certainty, provide a definitive diagnosis, or prescribe treatment."
"Explicitly state uncertainty and never invent findings."
```

**این درست است.** تحلیل AI باید:
- **کمکی** باشد، نه تشخیصی
- همیشه «مطمئن نیستم» بگوید
- دندانپزشک تأیید نهایی کند

**هشدار:** مدل‌های بینایی در تصاویر رادیولوژی **خطای زیادی** دارند.
MedGemma بهتر است ولی هنوز قابل اعتماد برای تشخیص قطعی نیست.

---

# ۸) سؤال‌هایی که باید پاسخ دهید

قبل از پیاده‌سازی:

1. **کامپیوتر کلینیک کارت گرافیک دارد؟**
   - بله → MedGemma روی همان اجرا شود
   - نه → یک کامپیوتر با GPU لازم است، یا Gemini

2. **کلینیک اینترنت دارد؟**
   - نه → فقط MedGemma
   - بله → Gemini هم ممکن است

3. **رضایت بیمار دارید؟**
   - برای مدل ابری **لازم** است
   - برای مدل محلی لازم نیست

4. **هدف چیست؟**
   - کمک به دندانپزشک → ✅ خوب
   - تشخیص قطعی → ❌ هیچ مدلی این را نمی‌تواند

---

# منابع

| منبع | آدرس |
|---|---|
| MedGemma (گوگل) | https://developers.google.com/health-ai-developer-foundations/medgemma/model-card |
| MedGemma روی Hugging Face | https://huggingface.co/google/medgemma-27b-it |
| Gemini pricing | https://ai.google.dev/gemini-api/docs/pricing |
| Gemini rate limits | https://ai.google.dev/gemini-api/docs/rate-limits |
| OpenRad (۱۷۰۰ مدل رادیولوژی باز) | https://link.springer.com (مقاله‌ی ۲۰۲۶) |

---

# خلاصه

**بله، سه راه هست:**

| راه | هزینه | حریم خصوصی | سختی |
|---|---|---|---|
| **MedGemma** (محلی) | **رایگان** | ✅ عالی | سخت |
| Gemini (رایگان) | رایگان | ❌ ضعیف | آسان |
| OpenAI (فعلی) | پول | ❌ ضعیف | آسان |

**توصیه‌ی من:** اگر کلینیک GPU دارد → **MedGemma**.
اگر فقط برای آزمایش است → **Gemini**.

**مهم:** این موضوع **جدا** از کار فردا (شکل نمای OPG) است.
اول نمای رادیولوژی را درست کنیم، بعد AI.
